spring:
  cloud:
    function:
      definition: partyStarted
    stream:
      bindings:
        partyStarted-out-0:
          destination: party-started
        partyStarted-in-0:
          destination: party-started
          group: game
      kafka:
        binder:
          brokers: ${KAFKA_BOOTSTRAP_SERVERS}
